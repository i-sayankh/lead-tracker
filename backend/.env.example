# Postgres connection string. postgres:// and postgresql:// are normalized to postgresql+psycopg://
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/leads
# Comma-separated list of allowed browser origins (never "*")
CORS_ORIGINS=http://localhost:5173
# development | test | production
ENVIRONMENT=development
